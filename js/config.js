/**
 * Региональная привязка приложения.
 *
 * Путевик рассчитан на прогулки по Калининграду, поэтому карта открывается
 * здесь же. До первого фикса GPS пользователь должен увидеть знакомые улицы:
 * пустой океан или чужой город на старте выглядят как сломанное приложение.
 *
 * Сюда же складываем остальные региональные настройки, если появятся —
 * границы панорамирования, смещение поиска адресов.
 */

/** Центр Калининграда: район Кафедрального собора и площади Победы. */
export const HOME_CENTER = { lat: 54.7104, lng: 20.5100 };

/** Стартовый зум: город целиком помещается в экран телефона. */
export const HOME_ZOOM = 12;
